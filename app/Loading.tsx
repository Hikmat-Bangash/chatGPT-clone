import React from 'react'

type Props = {}

const loading = (props: Props) => {
  return (
    <div className='w-full h-screen bg-gray-500 fixed text-white'>loading...</div>
  )
}

export default loading